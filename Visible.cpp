#include "Visible.h"
#include <string>
using namespace std;

// makes empty strings that are the strings to run on visible
string Visible(string value)
{
    const string Lambda("\\");
    if(value.empty())
        value=lambda;
    return value;
}
